import { TypeName } from "./typeDomain"

export interface DamageMultipliers {
    types: string[];
    four: string[],
    two: string[],
    one: string[],
    half: string[],
    quarter: string[],
    zero: string[]
}